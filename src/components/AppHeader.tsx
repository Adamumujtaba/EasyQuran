import {ActivityIndicator, Text, View} from 'react-native';

export const AppHeader = () => {
  return (
    <View
      style={{
        // flex: 1,
        alignContent: 'center',
        // justifyContent: 'center',
        // backgroundColor: 'red',
      }}>
      <Text>Header</Text>
    </View>
  );
};
